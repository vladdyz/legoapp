<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>Lego Collection</title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/css/main.css">
    </head>
    <body>
        <div class="navbar bg-base-100 relative z-10">
        <%- include('partials/navbar', {page: '' }) %>
        </div>
        <div class="container mx-auto p-4 min-h-screen relative border border-purple-200 rounded-lg overflow-hidden" style="background-image: url('/clouds-by-zabacraft.gif'); background-size: cover;">
            <!-- Hero DaisyUI Element -->
            <div class="hero absolute inset-0 bg-base-200 rounded-lg p-6 hero-overlay bg-opacity-60">
                <div class="hero-content text-center text-neutral-content">
                    <div class="max-w-md">
                        <h1 class="text-5xl font-bold">Modify Lego Set</h1>
                        <p class="py-6">Use the form below to update the details of <%= set.name %></p>
                        <!--I'm putting the form in the hero element so it can use the background-->
                        <form method="post" id="addSetForm" action="/lego/editSet/<%= set.set_num %>" class="grid grid-cols-1 gap-4">
                            <div class="flex flex-col">
                                <label for="name" class="boldtext mb-1">Name of the Lego Set</label>
                                <input type="text" id="name" name="name" value="<%= set.name %>" required class="p-2 border rounded-lg">
                            </div>
                            <div class="flex flex-col md:flex-row md:space-x-4">
                                <div class="flex-1">
                                    <label for="year" class="boldtext mb-1">Release Year</label>
                                    <input type="text" id="year" name="year" value="<%= set.year %>" required class="p-2 border rounded-lg">
                                </div>
                                <div class="flex-1">
                                    <label for="num_parts" class="boldtext mb-1">Number of Parts</label>
                                    <input type="number" id="num_parts" name="num_parts" value="<%= set.num_parts %>" required class="p-2 border rounded-lg">
                                </div>
                            </div>
                            <div class="flex flex-col">
                                <label for="img_url" class="boldtext mb-1">Image URL</label>
                                <input type="url" id="img_url" name="img_url" value="<%= set.img_url %>" required class="p-2 border rounded-lg">
                            </div>
                            <div class="flex flex-col">
                                <label for="theme_id" class="boldtext mb-1">Theme</label>
                                <select id="theme_id" name="theme_id" placeholder="Select a Theme" required class="p-2 border rounded-lg">
                                    <% themes.forEach(theme=>{ %>
                                        <option <%= (set.theme_id == theme.id) ? "selected" : "" %> value="<%= theme.id %>">
                                          <%= theme.name %>
                                        </option>
                                      <% }) %>
                                      
                                </select>
                            </div>
                            <div class="flex flex-col">
                                <label for="set_num" class="boldtext mb-1">Set Number</label>
                                <input type="text" id="set_num" name="set_num" value="<%= set.set_num %>" required class="p-2 border rounded-lg" class="cursor-not-allowed" readonly>
                            </div>
                            <div class="flex flex-col md:flex-row md:space-x-4 justify-center">
                                <input type="submit" value="Update Set" class="py-2 px-4 bg-blue-500 text-white font-semibold rounded-lg shadow-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-75">
                            </form>
                            <button class="btn btn-error px-3 space-x-3"><a href="/lego/deleteSet/<%= set.set_num %>">Delete This Set</a></button>
                            </div>
                       
                        
                    </div>
                </div>
            </div>
        </div>
    </body>
</html>
