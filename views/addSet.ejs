<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>Lego Collection</title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/css/main.css">
    </head>
    <body>
        <div class="navbar bg-base-100 relative z-10">
        <%- include('partials/navbar', {page: '/lego/addSet' }) %>
            </div>
        <div class="container mx-auto p-4 min-h-screen relative border border-purple-200 rounded-lg overflow-hidden" style="background-image: url('/addSet-bg.jpg'); background-size: cover;">
            <!-- Hero DaisyUI Element -->
            <div class="hero absolute inset-0 bg-base-200 rounded-lg p-6 hero-overlay bg-opacity-60">
                <div class="hero-content text-center text-neutral-content">
                    <div class="max-w-md">
                        <h1 class="text-5xl font-bold">Add Lego Set</h1>
                        <p class="py-6">Use the request form below to provide the necessary details</p>
                        <!--I'm putting the form in the hero element so it can use the background-->
                        <form method="post" id="addSetForm" action="/lego/addSet" class="grid grid-cols-1 gap-4">
                            <div class="flex flex-col">
                                <label for="name" class="boldtext mb-1">Name of the Lego Set</label>
                                <input type="text" id="name" name="name" placeholder="Ex: Star Wars Holiday Special Set" required class="p-2 border rounded-lg">
                            </div>
                            <div class="flex flex-col md:flex-row md:space-x-4">
                                <div class="flex-1">
                                    <label for="year" class="boldtext mb-1">Release Year</label>
                                    <input type="text" id="year" name="year" placeholder="Ex: 1994" required class="p-2 border rounded-lg">
                                </div>
                                <div class="flex-1">
                                    <label for="num_parts" class="boldtext mb-1">Number of Parts</label>
                                    <input type="number" id="num_parts" name="num_parts" placeholder="Enter a number" required class="p-2 border rounded-lg">
                                </div>
                            </div>
                            <div class="flex flex-col">
                                <label for="img_url" class="boldtext mb-1">Image URL</label>
                                <input type="url" id="img_url" name="img_url" placeholder="Enter the source URL of the set image" required class="p-2 border rounded-lg">
                            </div>
                            <div class="flex flex-col">
                                <label for="theme_id" class="boldtext mb-1">Theme</label>
                                <select id="theme_id" name="theme_id" placeholder="Select a Theme" required class="p-2 border rounded-lg">
                                    <% themes.forEach(theme => { %>
                                        <option value="<%= theme.id %>">
                                            <%= theme.name %>
                                        </option>
                                    <% }) %>
                                </select>
                            </div>
                            <div class="flex flex-col">
                                <label for="set_num" class="boldtext mb-1">Set Number</label>
                                <input type="text" id="set_num" name="set_num" placeholder="Ex: 1337-42" required class="p-2 border rounded-lg">
                            </div>
                            <div class="flex justify-center">
                                <input type="submit" value="Submit" class="py-2 px-4 bg-blue-500 text-white font-semibold rounded-lg shadow-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-75">
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </body>
</html>
